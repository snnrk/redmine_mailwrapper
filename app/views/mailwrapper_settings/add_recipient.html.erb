<h2><%= l(:label_mailwrapper_recipient_new) %></h2>

<% labelled_tabular_form_for :mailwrapper_recipient, @recipient, :url => {:action => 'add_recipient', :project_id => @project} do |f| %>
  <%= error_messages_for 'mailwrapper_recipient' %>
  <%= render :partial => 'form_recipient', :locals => { :f => f } %>
  <%= submit_tag l(:button_create) %>
<% end %>

