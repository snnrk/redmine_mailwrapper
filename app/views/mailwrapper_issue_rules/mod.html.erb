<h2><%= l(:label_mailwrapper_issue_rule_edit) %></h2>

<% labelled_form_for :mailwrapper_issue_rule, @mailwrapper_issue_rule, :url => {:action => 'mod', :project_id => @project, :issue_rule_id => @mailwrapper_issue_rule.id } do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= f.hidden_field :id, :value => @mailwrapper_issue_rule.id %>
  <%= submit_tag l(:button_update) %>
<% end %>

