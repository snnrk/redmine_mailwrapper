<h2><%= l(:label_mailwrapper_sender_new) %></h2>

<% labelled_form_for :mailwrapper_sender, @mailwrapper_sender, :url => {:action => 'add', :project_id => @project} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_create) %>
<% end %>

